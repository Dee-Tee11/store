STORE_CORS=http://localhost:8000,https://docs.medusajs.com
ADMIN_CORS=http://localhost:5173,http://localhost:9000,https://docs.medusajs.com
AUTH_CORS=http://localhost:5173,http://localhost:9000,https://docs.medusajs.com
JWT_SECRET=supersecret
COOKIE_SECRET=supersecret
DATABASE_URL=
DB_NAME=medusa-backend
RESEND_API_KEY=
RESEND_FROM_EMAIL=South Store <orders@southstore.net>

# Stripe — Dashboard > Developers > API keys (modo Test)
STRIPE_API_KEY=
# Sai do output de: stripe listen --forward-to localhost:9000/hooks/payment/stripe_stripe
STRIPE_WEBHOOK_SECRET=

# Emails transaccionais (ver src/subscribers/order-placed.ts)
# Para onde vai o aviso de nova encomenda. Aceita vários, separados por vírgula.
# Se ficar vazio, cai no RESEND_FROM_EMAIL.
ORDER_NOTIFICATION_EMAIL=
# Usado no link "Open in Admin" do email interno.
MEDUSA_BACKEND_URL=http://localhost:9000
# Nome da loja no cabeçalho dos emails.
STORE_NAME=South Store
# Usado no link do email de recuperação de password.
STOREFRONT_URL=http://localhost:8000
