# O @medusajs/icons declara peer de react@^18.3.1, mas o projecto corre em
# React 19 (exigido pelo Next 15). O conflito é só na declaração — os ícones
# funcionam em 19. Sem esta linha, o `npm ci` falha no build da Cloudflare.
legacy-peer-deps=true
